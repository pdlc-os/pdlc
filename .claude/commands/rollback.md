---
name: rollback
description: "Revert a shipped feature — alias for /pdlc rollback"
argument-hint: <feature-name>
---

Read `${PDLC_PLUGIN_ROOT}/skills/rollback/SKILL.md` and execute it completely. Pass `$ARGUMENTS` through verbatim.

This is a thin alias for `/pdlc rollback`. All path-resolution rules from `${PDLC_PLUGIN_ROOT}/.claude/commands/pdlc.md` apply.
